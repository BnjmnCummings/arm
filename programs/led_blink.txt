COMMENTED ASSEMBLY IN LED_BLINK.S
// load address of: GPIO pins 0 - 9 mode of input/output
x0 = 0x3f20000
ldr w1, #64
// store 64 into the adress which sets pin 2 to output and all others to input
str w1, [x0]

// load address of GPSET into x2
x2 = 0x3f20001c
// store 4 in w3 which can be used to flip the bit to on or off
ldr w3, #4

// load address of GPCLR into x2
x4 = 0x3f200028

// branch to on
b on

// run a sub operation a bunch of times then go to off (create a wait)
waitAfterOn:
sub w10, w10, #1
b.ne waitAfterOn
b off

// store 4 in GPSET to set pin 2 to high then call waitAfterOn
on:
str w3, [x2]
movk w10, #50, lsl #16
b waitAfterOn

// run a sub operation a bunch of times then go to on (create a wait)
waitAfterOff:
sub w10, w10, #1
b.ne waitAfterOff
b on

// store 4 in GPCLR to set pin 2 to low then call waitAfterOff
off:
str w3, [x4]
movk w10, #50, lsl #16
b waitAfterOff

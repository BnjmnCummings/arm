TODO(Implementation of part III goes here)
Should follow:
    select an arbitrary pin p
    continuously:
        set p to output a high signal (GPSET)
        wait (branches)
        set p to low signal (GPCLR)
        wait (branches)